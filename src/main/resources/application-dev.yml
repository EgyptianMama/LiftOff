# ============================
# Dev Profile Overrides
# ============================

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    dev.liftoff: DEBUG
    org.hibernate.SQL: DEBUG
    org.flywaydb: DEBUG
